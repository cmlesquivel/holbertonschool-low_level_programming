#include "holberton.h"


/**
 * mul - return the product of two numbers
 * @a: number one to multiply
 * @b: number two to multiply
 * Return: the product of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
