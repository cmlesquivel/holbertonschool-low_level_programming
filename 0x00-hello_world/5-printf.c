#include <stdio.h>

/**
 *main -entry point
 *
 *return :Always 0 (success)
 */

int main(void)
{
char str[80] = "with proper grammar, but the outcome is a piece of art,";
printf("%s \n", str);  
return (0);
}
